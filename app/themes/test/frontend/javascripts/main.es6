// Locomotive SiteBuilder - put your frontend JS code here
var process = require('process'); // Avoid warnings if the file is empty

// Example:

// import $ from 'jquery';
// window.$ = window.jQuery = $;
